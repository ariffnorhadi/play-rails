<h1>Articles</h1>

Our blog has <%= Article.public_count %> article and still counting!

<h2>Public</h2>
<ul>
  <% @articles.each do |article| %>
    <% if article.public? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<h2>Archived</h2>
<ul>
  <% @articles.each do |article| %>
    <% if article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<h2>Private</h2>
<ul>
  <% @articles.each do |article| %>
    <% if article.private? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>